<?xml version="1.0"?>
<odoo>

    <record id="vehicle_repair_action" model="ir.actions.act_window">
        <field name="name">vehicle_repair</field>
        <field name="res_model">vehicle.repair</field>
        <field name="view_mode">list,form</field>
    </record>

        <record id="vehicle_repair_view" model="ir.ui.view">
            <field name="name">vehicle_repair</field>
            <field name="model">vehicle.repair</field>
            <field name="arch" type="xml">
                <list string="vehicle_repair_management">
                    <field name="partner_id"/>
                    <field name="active"/>
                    <field name="mobile_no"/>
                    <field name="salesperson_id"/>
                    <field name="estimated_amount"/>
                    <field name="tag_ids"/>

                </list>
            </field>
        </record>


    <record id="vehicle_repair_view_form" model="ir.ui.view">
        <field name="name">vehicle_repair_form</field>
        <field name="model">vehicle.repair</field>
        <field name="arch" type="xml">
            <form string="vehicle repair management" >

                <sheet>
                    <header>
                    <button name="action_confirm" type="object" string="Confirm" class="oe_highlight"
                            invisible="state in ('in progress','ready_for_delivery','done','cancel')"/>
                    <button name="action_ready_for_delivery" type="object" string="Ready for Delivery" class="oe_highlight"
                            invisible="state in ('cancel','draft','ready for delivery','done')"/>

                        <button name="action_done" type="object" string="done" class="oe_highlight" invisible="state in ('cancel','draft','in progress','done')"/>

                    <button name="action_cancel" type="object" string="Cancel"
                            invisible="state in ('done', 'cancel')"/>
                        <field name="state" widget="statusbar" statusbar_visible="draft, in progress, ready for delivery, done, canceled"/>

                      </header>

                    <div class="title">
                        <h1><field name="reference" placeholder="Sequence no"/></h1>
                    </div>

                    <field name="image_1920"  class="oe_avatar" widget="image"/>

                    <group>
                        <group>
                            <field name="partner_id"
                               widget="res_partner_many2one"
                               context="{'res_partner_search_mode': 'customer', 'show_address': 1, 'show_vat': True}"
                               placeholder=" customer..." readonly="state in ('in progress')"/>

                            <field name="active"/>
                            <field name="tag_ids"  widget="many2many_tags" options="{'color_field': 'color'}" readonly="state in ('in progress')"/>
                            <field name="company_id"/>
                            <field name="estimated_amount"/>

                        </group>
                        <group>
                            <field name="start_date" readonly="state in ('in progress')"/>
                            <field name="vehicle_type_id" readonly="state in ('in progress')"/>
                            <field name="vehicle_model_id" readonly="state in ('in progress')"/>
                            <field name="vehicle_number" />
                            <field name="salesperson_id" />
                            <field name="mobile_no" placeholder=" customer number"/>
"
                        </group>

                    </group>

                    <notebook>
                        <page string="Customer Complaint:" >
                            <field name="customer_complaint" placeholder="Type issue"/>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>

            </form>
        </field>
    </record>
<record id="seq_vehicle_repair" model="ir.sequence">
    <field name="name">Vehicle Repair Sequence</field>
    <field name="code">vehicle.repair.management</field>
    <field name="prefix">REPAIR//</field>
    <field name="padding">5</field>
</record>

</odoo>

